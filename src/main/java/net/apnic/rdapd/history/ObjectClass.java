package net.apnic.rdapd.history;

/**
 * RDAP object classes.  Each record from the registry's history must be
 * mapped to one of these types.
 */
public enum ObjectClass {
    IP_NETWORK,
    DOMAIN,
    AUT_NUM,
    ENTITY
}
