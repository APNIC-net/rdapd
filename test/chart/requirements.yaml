# Remember to update local subcharts after modifying this file
# eg, helm dependency update .

# TODO: discuss saving dep charts in repo (no, tar, unpacked ???)

dependencies:
  - name: rdapd
    version: 1.1.1
    repository: http://example.com/charts
